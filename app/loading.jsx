import React from 'react';
import LoadingComponent from './components/LoadingComponent';

const Loading = () => {
  return <LoadingComponent size={500} text={'Cargando...'} textSize={'3xl'} />;
};

export default Loading;
